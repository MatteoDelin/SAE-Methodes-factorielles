````r
---
title: "Rapport Planet"
output: html_document
date: "2026-03-26"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
library(readxl)
library(FactoMineR)
library(factoextra)
library(corrplot)
library(ggplot2)
library(tidyr)
library(dplyr)
```

## 1. Import des données

```{r import}
planet <- read_xlsx("PlanetDataSet (pretraitement).xlsx",
                    sheet = "nasa_exoplanet_intelligence")
cat("Dimensions initiales :", nrow(planet), "lignes x", ncol(planet), "colonnes\n")
```

## 2. Traduction des noms de variables en français

```{r traduction}
planet <- planet %>%
  rename(
    # Identifiants
    nom_planete              = planet_name,
    etoile_hote              = host_star,
    nb_etoiles               = n_stars,
    nb_planetes              = n_planets,
    # Découverte
    methode_decouverte       = discovery_method,
    annee_decouverte         = disc_year,
    installation_decouverte  = disc_facility,
    # Propriétés de la planète
    periode_orbitale_jours   = orbital_period_days,
    rayon_planete_terre      = planet_radius_earth,
    masse_planete_terre      = planet_mass_earth,
    temp_equilibre_k         = equilibrium_temp_k,
    excentricite_orbitale    = orbital_eccentricity,
    demi_grand_axe_ua        = semi_major_axis_au,
    # Propriétés de l'étoile
    temp_etoile_k            = star_temp_k,
    rayon_etoile_soleil      = star_radius_sun,
    masse_etoile_soleil      = star_mass_sun,
    age_etoile_gyr           = star_age_gyr,
    gravite_surface_etoile   = star_surface_gravity,
    metallicite_etoile       = star_metallicity,
    distance_terre_pc        = dist_from_earth_pc,
    magnitude_visuelle       = star_vmag,
    ascension_droite         = ra,
    declinaison              = dec,
    # Flags et catégories
    planete_controversee     = controversial_flag,
    type_planete             = planet_type,
    zone_habitable           = habitable_zone_flag,
    systeme_multi_planetes   = multi_planet_system,
    decouverte_recente       = is_recent_discovery,
    categorie_distance       = dist_category,
    type_etoile              = star_type,
    categorie_periode        = orbital_period_cat
  )

cat("Colonnes après renommage :\n")
print(names(planet))
```

## 3. Exploration visuelle — histogrammes de toutes les variables

On trace les histogrammes de toutes les variables numériques AVANT tout nettoyage
pour justifier empiriquement les choix d'inclusion et d'exclusion.

```{r histogrammes_bruts, fig.height=12, fig.width=14}
vars_num <- names(planet)[sapply(planet, is.numeric)]

planet %>%
  select(all_of(vars_num)) %>%
  pivot_longer(cols = everything(), names_to = "variable", values_to = "valeur") %>%
  ggplot(aes(x = valeur)) +
  geom_histogram(fill = "steelblue", color = "white", bins = 40, na.rm = TRUE) +
  facet_wrap(~ variable, scales = "free", ncol = 4) +
  theme_minimal() +
  labs(title = "Distribution brute de toutes les variables numériques",
       x = "", y = "Effectif")
```

Conclusions des histogrammes :

- `temp_equilibre_k` (~25% de NA) et `age_etoile_gyr` (~21% de NA) :
  histogrammes très lacunaires → exclus des variables actives
- `nb_etoiles` et `nb_planetes` : distributions quasi-constantes (écrasées sur 1),
  variance trop faible pour contribuer à l'ACP → exclus
- `planete_controversee` : quasi-constante (99% de 0) → exclue
- `ascension_droite` et `declinaison` : coordonnées célestes sans lien
  avec la problématique → exclues
- `magnitude_visuelle` : dépend de la distance à la Terre, pas une propriété
  intrinsèque de l'étoile → exclue
- `periode_orbitale_jours`, `demi_grand_axe_ua`, `masse_planete_terre`,
  `rayon_planete_terre` : distributions très asymétriques avec valeurs
  extrêmes → transformation log

## 4. Transformations logarithmiques des variables asymétriques

`log1p(x)` = `log(x + 1)`. Le +1 évite log(0) qui est indéfini.
Ces variables ont des distributions en "queue de comète" (ex : periode_orbitale_jours
va de 0.09 à 400 millions de jours). Sans transformation, les outliers tireraient
artificiellement un axe ACP vers eux. Le log compresse les grandes valeurs
et raisonne en ordres de grandeur.

```{r log_transform}
planet$periode_orbitale_jours <- log1p(planet$periode_orbitale_jours)
planet$demi_grand_axe_ua      <- log1p(planet$demi_grand_axe_ua)
planet$masse_planete_terre    <- log1p(planet$masse_planete_terre)
planet$rayon_planete_terre    <- log1p(planet$rayon_planete_terre)
```

```{r histogrammes_apres_log, fig.height=6, fig.width=14}
vars_log <- c("periode_orbitale_jours", "demi_grand_axe_ua",
              "masse_planete_terre",    "rayon_planete_terre")

planet %>%
  select(all_of(vars_log)) %>%
  pivot_longer(everything(), names_to = "variable", values_to = "valeur") %>%
  ggplot(aes(x = valeur)) +
  geom_histogram(fill = "darkorange", color = "white", bins = 40, na.rm = TRUE) +
  facet_wrap(~ variable, scales = "free", ncol = 4) +
  theme_minimal() +
  labs(title = "Distribution après transformation log (variables asymétriques)",
       x = "", y = "Effectif")
```

## 5. Sélection des variables et nettoyage ciblé

On sélectionne d'abord les variables pertinentes pour la problématique,
PUIS on supprime les NA — et non l'inverse — pour minimiser la perte d'observations.

`annee_decouverte` et `distance_terre_pc` sont passées en quantitatives
supplémentaires (et non actives) car elles décrivent les conditions d'observation
et non les propriétés physiques intrinsèques. Elles serviront à détecter
d'éventuels biais d'observation après coup.

```{r selection}
# Variables actives (entrent dans le calcul de l'ACP)
vars_actives <- c(
  # Propriétés de la planète
  "periode_orbitale_jours", "rayon_planete_terre", "masse_planete_terre",
  "excentricite_orbitale",  "demi_grand_axe_ua",
  # Propriétés de l'étoile
  "temp_etoile_k", "rayon_etoile_soleil", "masse_etoile_soleil",
  "metallicite_etoile", "gravite_surface_etoile"
)

# Variables illustratives quantitatives (projetées a posteriori, hors calcul)
# Servent à détecter des biais d'observation
vars_quanti_sup <- c("annee_decouverte", "distance_terre_pc")

# Variables illustratives qualitatives (projetées a posteriori, hors calcul)
vars_quali_sup <- c("type_planete", "type_etoile", "methode_decouverte",
                    "zone_habitable", "systeme_multi_planetes")

# Sélection et nettoyage
planet_sel   <- planet[, c(vars_actives, vars_quanti_sup, vars_quali_sup)]
planet_clean <- na.omit(planet_sel)

# Conversion des booléens en facteurs pour FactoMineR
planet_clean$zone_habitable         <- as.factor(planet_clean$zone_habitable)
planet_clean$systeme_multi_planetes <- as.factor(planet_clean$systeme_multi_planetes)

cat("Lignes initiales       :", nrow(planet), "\n")
cat("Après sélection        :", nrow(planet_sel), "\n")
cat("Après suppression NA   :", nrow(planet_clean), "\n")
cat("Lignes perdues         :", nrow(planet_sel) - nrow(planet_clean), "\n")
```

## 6. Matrice de corrélation

Avant l'ACP, on visualise les corrélations pour repérer les relations linéaires
fortes entre variables actives.

```{r corrplot, fig.height=7, fig.width=7}
corrplot(cor(planet_clean[, vars_actives]),
         method      = "color",
         type        = "upper",
         tl.cex      = 0.8,
         addCoef.col = "black",
         number.cex  = 0.6,
         title       = "Matrice de corrélation des variables actives",
         mar         = c(0, 0, 1, 0))
```

On observe plusieurs corrélations notables :

Corrélations fortes positives (> 0.8) :
- `periode_orbitale_jours` et `demi_grand_axe_ua` (0.91) : ces deux variables
  évoluent très fortement dans le même sens. L'ACP les placera probablement
  sur le même axe.
- `rayon_planete_terre` et `masse_planete_terre` (0.91) : même observation.

Corrélations fortes négatives (< -0.6) :
- `rayon_etoile_soleil` et `gravite_surface_etoile` (-0.75) : quand le rayon
  de l'étoile augmente, sa gravité de surface tend à diminuer.
- `masse_etoile_soleil` et `gravite_surface_etoile` (-0.66) : même tendance.

Ces corrélations élevées suggèrent que certaines dimensions seront redondantes.
L'ACP va précisément permettre de résumer cette structure en axes indépendants.

## 7. ACP

`scale.unit = TRUE` centre et réduit toutes les variables (moyenne = 0, écart-type = 1).
C'est indispensable ici car les variables ont des unités très différentes
(jours, kelvin, masses solaires...). Sans cette option, les variables avec les plus
grandes valeurs absolues domineraient artificiellement l'ACP, indépendamment
de toute corrélation réelle.

```{r acp}
# Indices des colonnes supplémentaires dans le tableau
# Ordre obligatoire : actives | quanti.sup | quali.sup
idx_quanti_sup <- (length(vars_actives) + 1):(length(vars_actives) +
                                               length(vars_quanti_sup))
idx_quali_sup  <- (length(vars_actives) + length(vars_quanti_sup) + 1):ncol(planet_clean)

res.pca <- PCA(planet_clean,
               quanti.sup = idx_quanti_sup,
               quali.sup  = idx_quali_sup,
               scale.unit = TRUE,
               ncp        = 3,
               graph      = FALSE)

res.pca$eig
```

## 8. Eboulis des valeurs propres

Trois critères pour choisir le nombre de dimensions à retenir :
1. Valeur propre > 1 (critère de Kaiser)
2. Inertie cumulée >= 60%
3. Coude visuel sur l'éboulis

```{r eboulis}
fviz_eig(res.pca, addlabels = TRUE, ncp = 10,
         main = "Eboulis des valeurs propres")
```

## 9. Choix des axes retenus

L'éboulis met en évidence la structure suivante :
- Dim 1 : 40,1% — axe dominant, très nettement au-dessus des autres
- Dim 2 : 16,9% — chute importante par rapport à la dimension 1
- Dim 3 : 14,1% — légèrement inférieure à la dimension 2
- Dim 4+  : décroissance régulière et faible (9,5%, 7,2%, 6%...)

Critère de Kaiser (valeur propre > 1, soit > 10% ici) :
dimensions 1, 2 et 3 dépassent ce seuil → 3 axes.

Critère de l'inertie cumulée :
- Axes 1+2   = 57,0% → sous le seuil de 60%
- Axes 1+2+3 = 71,1% → seuil atteint → 3 axes.

Critère du coude :
la rupture de pente la plus nette se situe entre la dimension 3 (14,1%)
et la dimension 4 (9,5%), où la courbe s'aplatit durablement → 3 axes.

Les trois critères convergent vers 3 axes. Dans la suite, on analyse le plan
factoriel des axes 1 et 2 (57% de l'inertie) puis l'axe 3 (14,1%) séparément.

## 10. Contribution des variables par axe

Plus le score est élevé, plus la variable contribue à construire cet axe.
La ligne rouge indique la contribution moyenne attendue si toutes les variables
contribuaient également (1/nb_variables * 100 = 10% ici).

```{r contrib}
# Tableau des contributions pour les 3 axes
round(res.pca$var$contrib[, 1:3], 2)

# Graphiques par axe
for (i in 1:3) {
  print(fviz_contrib(res.pca, choice = "var", axes = i,
                     title = paste("Contribution des variables — Axe", i)))
}
```

## 11. Cercles de corrélation — Plan (1,2)

Sur le cercle de corrélation :
- Une flèche proche du bord = variable bien représentée sur ce plan → interprétable
- Une flèche courte (proche du centre) = variable mal représentée → ne pas interpréter
- Deux flèches dans la même direction = variables corrélées positivement
- Deux flèches opposées = variables corrélées négativement
- Deux flèches perpendiculaires = variables indépendantes

```{r cercles_12, fig.height=6, fig.width=8}
fviz_pca_var(res.pca, axes = c(1, 2),
             col.var = "contrib",
             repel   = TRUE,
             title   = "Cercle de corrélation — Axes 1 & 2")
```

## 12. Graphique des variables — Qualité de représentation

La couleur (cos2) indique la qualité de représentation de chaque variable
sur ce plan : bleu foncé = bien représentée, gris = mal représentée.

```{r graph_var_cos2, fig.height=6, fig.width=8}
fviz_pca_var(res.pca,
             axes          = c(1, 2),
             col.var       = "cos2",
             gradient.cols = c("grey", "steelblue", "darkblue"),
             repel         = TRUE,
             title         = "Qualité de représentation des variables — Axes 1 & 2")
```

## 13. Projection des variables illustratives — Plan (1,2)

### Variables quantitatives supplémentaires

Ces variables n'ont pas servi à construire les axes. Leurs coordonnées
(assimilables à des corrélations, entre -1 et 1) indiquent si elles sont
associées à un axe. Une valeur proche de 0 = pas de biais d'observation.
Une valeur proche de 1 ou -1 = biais potentiel à mentionner comme limite.

```{r proj_quanti_sup}
round(res.pca$quanti.sup$coord, 3)
```

### Variables qualitatives supplémentaires

Pour chaque variable, les ellipses de confiance à 95% entourent les individus
de chaque modalité. Des ellipses séparées = cette variable discrimine bien
les individus sur ce plan.

```{r proj_quali_sup_12, fig.height=6, fig.width=10}
for (v in vars_quali_sup) {
  print(
    fviz_pca_ind(res.pca,
                 axes          = c(1, 2),
                 habillage     = v,
                 label         = "none",
                 alpha.ind     = 0.3,
                 addEllipses   = TRUE,
                 ellipse.level = 0.95,
                 title         = paste("Plan (1,2) — Individus colorés par :", v))
  )
}
```

## 14. Graphique des individus — Plan (1,2)

Chaque point = une exoplanète, positionnée selon ses scores factoriels.
Les ellipses de confiance à 95% permettent de voir si les types de planètes
se distinguent bien sur ce plan.

```{r individus_12, fig.height=6, fig.width=10}
fviz_pca_ind(res.pca,
             axes          = c(1, 2),
             habillage     = "type_planete",
             label         = "none",
             alpha.ind     = 0.4,
             addEllipses   = TRUE,
             ellipse.level = 0.95,
             title         = "Individus colorés par type de planète — Plan (1,2)")
```

## 15. Biplot — Plan (1,2)

Le biplot superpose variables (flèches) et individus (points) sur le même graphique.
Un individu dans la direction d'une flèche = valeur élevée pour cette variable.
Un individu dans la direction opposée = valeur faible.

```{r biplot_12, fig.height=6, fig.width=10}
fviz_pca_biplot(res.pca,
                axes      = c(1, 2),
                col.ind   = planet_clean$type_planete,
                col.var   = "black",
                label     = "var",
                repel     = TRUE,
                alpha.ind = 0.3,
                title     = "Biplot — Plan (1,2)")
```

## 16. Analyse de l'axe 3 — Cercles de corrélation

L'axe 3 (14,1% d'inertie) est retenu par les trois critères statistiques.
Il capte une structure complémentaire aux axes 1 et 2 — des variables ou
des groupes qui ne se distinguaient pas sur le plan (1,2) peuvent apparaître
clairement ici.

```{r cercles_axe3, fig.height=6, fig.width=12}
fviz_pca_var(res.pca, axes = c(1, 3),
             col.var = "contrib",
             repel   = TRUE,
             title   = "Cercle de corrélation — Axes 1 & 3")

fviz_pca_var(res.pca, axes = c(2, 3),
             col.var = "contrib",
             repel   = TRUE,
             title   = "Cercle de corrélation — Axes 2 & 3")
```

## 17. Contribution des variables à l'axe 3

```{r contrib_axe3}
print(fviz_contrib(res.pca, choice = "var", axes = 3,
                   title = "Contribution des variables — Axe 3"))
```

## 18. Projection des variables illustratives — Plan (1,3)

On reprojette les variables illustratives sur les plans contenant l'axe 3
pour voir si elles discriminent différemment que sur le plan (1,2).
Des groupes qui se chevauchaient sur (1,2) peuvent se séparer ici.

```{r proj_quali_axe3, fig.height=6, fig.width=10}
for (v in vars_quali_sup) {
  print(
    fviz_pca_ind(res.pca,
                 axes          = c(1, 3),
                 habillage     = v,
                 label         = "none",
                 alpha.ind     = 0.3,
                 addEllipses   = TRUE,
                 ellipse.level = 0.95,
                 title         = paste("Plan (1,3) — Individus colorés par :", v))
  )
}
```

## 19. Graphique des individus — Plan (1,3)

```{r individus_13, fig.height=6, fig.width=10}
fviz_pca_ind(res.pca,
             axes          = c(1, 3),
             habillage     = "type_planete",
             label         = "none",
             alpha.ind     = 0.4,
             addEllipses   = TRUE,
             ellipse.level = 0.95,
             title         = "Individus colorés par type de planète — Plan (1,3)")
```

## 20. Biplot — Plan (1,3)

```{r biplot_13, fig.height=6, fig.width=10}
fviz_pca_biplot(res.pca,
                axes      = c(1, 3),
                col.ind   = planet_clean$type_planete,
                col.var   = "black",
                label     = "var",
                repel     = TRUE,
                alpha.ind = 0.3,
                title     = "Biplot — Plan (1,3)")
```
````